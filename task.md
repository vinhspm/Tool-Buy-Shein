# Task List: Implement Profile Quota Assignment

- [x] Update `server.js` (`/api/start` logic and `DEFAULT_CONFIG`).
- [x] Update `public/index.html` (Add UI input for `maxProductsPerProfile`).
- [x] Update `public/app.js` (Read/write `maxProductsPerProfile` from API).
